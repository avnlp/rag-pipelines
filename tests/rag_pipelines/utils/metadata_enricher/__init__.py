"""Test suite for metadata enricher utilities.

Tests the three-layer metadata enrichment pipeline that transforms documents
into structured metadata through structural (basic properties), dynamic (user-defined),
and fixed (predefined) schema enrichment layers.
"""

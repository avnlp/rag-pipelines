"""Tests for the StructuralMetadataEnricher layer.

Tests the structural enrichment layer that extracts basic document properties
(word count, character count, language, content hash) and applies document context
(page number, section title) without requiring LLM inference.
"""
